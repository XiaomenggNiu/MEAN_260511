import { createAction } from "@ngrx/store";

// [source] Event
export const increment = createAction('[Counter Component] Increment');
export const decrement = createAction('[Counter Component] Devrement');